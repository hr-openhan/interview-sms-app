{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["public/**", "templates.json"]
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }]
}
